# Sounds

## checkin.mp3

<!-- REPLACE: Replace checkin.mp3 with the actual check-in chime sound.
     Specs:
       - Duration: 0.4–1.5 seconds (shorter = snappier feel)
       - Style: Friendly synth pop / futuristic AI bleep / soft bell
       - Format: MP3 or OGG (Howler.js supports both)
       - Volume: Normalised to -12 dBFS recommended (Howler plays at 0.7 volume)
     Used by: src/lib/sound.ts → playCheckin()
     If adding OGG fallback, update src/lib/sound.ts:
       src: ['/sounds/checkin.ogg', '/sounds/checkin.mp3']
-->

The current file is a 0-byte placeholder that will produce no sound.
